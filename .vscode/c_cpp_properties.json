{
    "configurations": [
        {
            "name": "Custom",

            "cStandard": "${default}",
            "cppStandard": "${default}",
            "compilerPath": "${default}",
            "intelliSenseMode": "${default}",
            
            "includePath": ["${default}"],
            "defines": [
                "${default}",
                "BUILD_DLL"
            ]
        }
    ],
    "version": 4
}