{
    "configurations": [
        {
            "name": "Custom",

            "cStandard": "${default}",
            "cppStandard": "${default}",
            "compilerPath": "${default}",
            "intelliSenseMode": "${default}",
            
            "includePath": ["${default}"],
            "defines": [
                "${default}",
                "GES_PRECOMPILED_HEADER",
                "WIN32_LEAN_AND_MEAN"
            ]
        }
    ],
    "version": 4
}
